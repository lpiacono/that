class hygieneCategory:
    
    def __init__(self, name, sensors):
        self.name = name
        self.sensors = []
        self.slidelayout = slidenumber

    #We may want to objectify the Data processing call. Make it a method, instead of calling function